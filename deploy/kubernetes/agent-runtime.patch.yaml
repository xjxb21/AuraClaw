apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-runtime
spec:
  template:
    spec:
      containers:
        - name: agent-runtime
          env:
            - name: AURACLAW_RUNTIME_INSTANCE_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
