# 剩余 Skill / MCP issues 验收边界复核（2026-09-04）

采用用户在 #90 确认的标准：项目自身职责已实现并验证，下游 MCP 业务数据、输出契约或 Java 业务上下文问题不阻塞相应代码修复关闭；自身生命周期、迁移、恢复和审批验收仍保留。只读调用截图不作为未执行场景的验证证据。

| 仓库 | Issue | 结论 | 依据或剩余项 |
| --- | --- | --- | --- |
| AuraClaw | #89 | 保持开放 | 尚有 AuraSkill 源码 Git 归属/交付、Admission 复核、实际安装 pin 迁移及旧版引用核对。外部数据集不足不再单独阻塞 AuraClaw 能力验收，但这些安装/交付工作未完成，不能关闭。 |
| AuraClaw | #91 | 保持开放 | 三仓库代码和迁移已交付并部署；下游业务返回成功不再是关闭条件。仍缺全部 enabled Server 的新 generation 完整对账及逐副本路由验证，另有原计划的真实 KingBase 兼容验收未记录。这些属于本项目部署/迁移验收，不由一次工具调用覆盖。 |
| AuraClaw | #92 | 保持开放 | 后端与 AuraX 已实现并部署，仍缺三档模式实际自动审核、转人工、恢复及硬性 deny 联调闭环。只读调用不触发审批并未覆盖 require_approval 场景；该剩余项属于 AuraClaw 自身行为。 |
| AuraClaw | #93 | 完成，可关闭 | 参数、Schema、错误分类及标准 HTTP 会话的代码和集成验证已完成并部署；阶段 G/H 有完整证据，e66c562 补充模型参数提示，#101 已处理重复只读调用预算。按用户确认的 #90 边界，下游输出 Schema、业务入参有效性与业务数据成功不再阻塞此 AuraClaw 修复。保留参数原样传输与输出校验，不把远端失败改成成功。 |
| AuraClaw | #94 | 保持开放 | 原子升级与自动物理清理代码、迁移、UI 已部署。现有证据仍为安装 pin=1.0.0 与新版 publication 不一致，6 次旧版 unknown 写调用待原结果核对，安装切换/引用释放/旧包及历史信息实际清除尚未验收。未知副作用不是普通业务报错，不能据新标准改写为已完成。 |
| AuraClaw | #95 | 保持开放 | 权威撤销查询及恢复代码已部署，仍缺真实运行中 revoke/pause/force uninstall 与 Canonical 引用释放联合验收。这是自身撤销和生命周期语义，不能用远端只读可达替代。 |
| AuraClaw | #96 | 保持开放 | 工作流状态、预算、unknown 恢复代码已部署，#101 已完成通用预算改善；仍缺旧在途写调用排空及与 #94 自动清理的现场闭环，聊天 completed 不代表声明式工作流的恢复/清理全部验收。 |
| AuraClaw | #97 | 保持开放 | 完整目标路由、双 HTTP MCP 同名工具及独立进程回归已有证据，代码已部署。剩余独立 Credential HTTP 部署拓扑下的多 owner/generation 切换与 #98/#99 恢复联合验收属于自身路由一致性，不以 Java 业务成功为门禁，但暂不能关闭。 |
| AuraClaw | #98 | 保持开放 | 撤销重试、隔离和跨副本清理实现已部署，独立进程测试已有 5 秒收敛证据；真实分离 Hands/Egress 部署拓扑的禁用/删除/恢复时限与残留清理仍需验收，业务可达不能证明这些故障场景。 |
| AuraClaw | #99 | 保持开放 | 冷快照安装、假就绪修复、0060 迁移和双进程恢复已有证据且已部署；真实分离部署拓扑下的双 Hands 冷启动/滚动恢复与 generation 撤销联验仍未完成，不能用一次正常调用推断所有副本正确。 |
| AuraClaw | #100 | 完成，可关闭 | 可信 tenant/user/dept 透传与持久化恢复已由 b87893c、正式 HTTP Hands→Gateway→Connector→Credential Proxy→真实测试 MCP 和 PostgreSQL 集成验证，阶段 B 记录 8 项集成、30 项身份契约和 66 项相关回归通过；修复已部署。Java 用户/部门事实源及其线程上下文问题不再阻塞 AuraClaw 透传修复。截图不是线上三个身份头逐值一致的独立证明，结论同时依据既有报文与恢复测试。 |
| AuraX | #5 | 保持开放 | 路径归一化已实现并部署，仍缺 direct/staged 实际发布闭环；此处接口衔接属于 AuraX/AuraClaw 自身流程，并非 Java MCP 业务错误。 |
| AuraX | #7 | 保持开放 | 仍缺刷新保留旧正文、四态渲染、发布后详情重新获取与跨客户端一致性验证；属于前端自身行为。 |
| AuraX | #11 | 保持开放 | 仍缺真实身份下筛选、分页和身份切换验证；与 MCP 下游无关。 |
| AuraX | #12 | 保持开放 | 下游 MCP 输出契约错误从工作台验收阻塞中移除；保留新版工作台整体真实环境验收待办。截图覆盖聊天和轨迹，不能自动覆盖任务页、管理页与 Skill 分层全部交互。 |
| AuraX | #13 | 保持开放 | 三档审批选择器已部署，仍需与 AuraClaw #92 完成实际审核、转人工、恢复和拒绝联调；只读免审批不足以完成此矩阵。 |

#100 中历史附带的长期 Vault 工作负载认证/续发仍是未完成运维事项，不随代码 issue 关闭而宣称解决；继续保留在 docs/operations/skill-mcp-test-rollout.md 的凭据运维记录中。此次不修改任何凭据或清理历史调用。
