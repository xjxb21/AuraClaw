# S5 测试报告

## 范围

S5 将生产部署实现收敛为 Docker Compose：12 服务副本与资源边界、Secret 文件挂载、独立
数据库角色、一次性迁移进程、内部网络/统一 ingress、蓝绿发布、扩缩容和故障恢复。

## 自动化覆盖

- `test_s5_production_deployment.py`：Compose 解析、12 服务副本/资源/身份/角色、安全选项、
  唯一入口、最小 Secret 分发、secret-file loader 与迁移发现。
- `scripts/compose_preflight.py`：不可变镜像、必填配置、统一 `AURACLAW_DATABASE_URL`、唯一高熵 workload
  token/lease key 与 Compose 解析的部署前 fail-fast 门禁。
- `test_postgres_s5_migration_runner.py`：真实临时 PostgreSQL 上的首次并发迁移、advisory
  lock、幂等重跑、checksum drift。
- S1–S4、M5–M8 测试继续覆盖版本化内部契约、完整任务链路、SSE replay、Delivery
  retry/DLQ/redrive、Projection rebuild、Retention、审计、owner kill 后 claim/lease
  接管、SeaweedFS、Vault、Kafka 和数据库角色边界。

## 当前门禁结果

- Pytest：133 项全部通过；新增 Task API 首次 `run.requested` ACL、Runtime 失败事实先写后
  ack、Ingress 动态 Docker DNS 覆盖。
- Integration：干净 `auraclaw_dev` 上 28 项均已验证通过；首次全量运行暴露清库后缺少
  retention seed 与 Vault 测试字段，恢复 4 条迁移种子并将本地
  `TEST_VAULT_CREDENTIAL_FIELD` 指向已存在的 `password` 字段后，失败项重跑通过。
- Ruff、Mypy（146 个源码文件）、10 条 import-linter、Release Gate、Compose config 均通过。
- `auraclaw:s5` 镜像构建通过；镜像为非 root 用户，`migrate` 与服务 CLI 入口可执行。
- migration runner 在隔离 PostgreSQL 的首次并发、14 条 migration、幂等和 drift 测试通过。
- production preflight、27 个 `0600` Secret 文件、既有库 `0014` baseline/status/幂等 up
  均通过。
- blue 完整拓扑共 29 个业务容器（12 服务）和 1 个 ingress 全部 healthy；真实 canary
  返回 `S5_COMPOSE_OK`，同一幂等键返回相同 Session/Run；清库、最终镜像全量重建后，
  最终 smoke 返回 `S5_FINAL_OK`。
- Session、Runtime、Orchestrator、Model、Hands、Policy、Credential、Artifact、Streaming、
  Delivery、Projection 各停 1 副本时，降级 canary 返回 `S5_DEGRADED_OK`，随后所有副本恢复。
- 隔离 green project 与 blue 共享 expand schema 并存，缩容 green canary 返回
  `S5_GREEN_OK`；仅替换 Task API 后 ingress 无重启自动重解析并恢复 `200 ready`。

在线 Projection rebuild 完成后，Canonical Result 仍恢复为 `S5_GREEN_OK`；Artifact retention
运维扫描完成且未误删对象，Projection poison count 为 0。Delivery DLQ/redrive、Vault/SeaweedFS、
Kafka、PostgreSQL 短暂失败与恢复由完整集成/故障注入套件覆盖。

演练同时发现并修复三个生产缺口：Task API 缺少 `run.requested` Session ACL、Runtime 异常先
ack assignment 却不写 Canonical `run.failed`、Nginx 缓存已替换的上游 IP。完整最终门禁、
Ops E2E 与外部依赖故障项均已通过。
